//------------------------------------------------------------------------------
// <auto-generated>
// Ce code a été généré par un outil.
//
// Les modifications apportées à ce fichier peuvent provoquer un comportement
// incorrect et seront perdues si le code est régénéré.
// </auto-generated>
//------------------------------------------------------------------------------

import { Component, OnInit } from '@angular/core';

import { PagedData } from '../../paged-data';
import { FournService } from './fourn.service';
import { Fourn } from '../../dtos';

@Component({
	selector: 'fourn-list',
	templateUrl: 'fourn-list.component.html',
	providers: [FournService]
})
export class FournListComponent implements OnInit {
	fourn: PagedData<Fourn> = {
		sort: 'LibFourn',
		pageIndex: 0,
		pageSize: 20
	}

	constructor(private fournService: FournService) {
	}

	ngOnInit() {
		this.loadFourn();
	}

	loadFourn(): void {
		const fields = 'CodFourn,LibFourn,EstActif';
		const skip = this.fourn.pageIndex * this.fourn.pageSize;
		this.fournService.selectFourn(fields, [this.fourn.sort], skip, this.fourn.pageSize)
			.then(r => this.fourn.data = r.Results);
	}

	deleteFourn(id: number): void {
		this.fournService.deleteFourn(id);
	}
}
