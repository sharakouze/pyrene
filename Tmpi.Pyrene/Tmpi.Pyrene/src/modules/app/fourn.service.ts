//------------------------------------------------------------------------------
// <auto-generated>
// Ce code a été généré par un outil.
//
// Les modifications apportées à ce fichier peuvent provoquer un comportement
// incorrect et seront perdues si le code est régénéré.
// </auto-generated>
//------------------------------------------------------------------------------

import { Injectable } from '@angular/core';

import { ServiceClient } from '../../service-client';
import { SelectFourn, SelectFournResponse, DeleteFourn } from '../../dtos';

@Injectable()
export class FournService extends ServiceClient {
	selectFourn(fields: string, sort: string[], skip?: number, take?: number): Promise<SelectFournResponse> {
		const req = new SelectFourn();
		req.Fields = fields;
		req.Sort = sort;
		if (skip) {
			req.Skip = skip;
		}
		if (take) {
			req.Take = take;
		}

		return this.client.get(req);
	}

	deleteFourn(id: number): Promise<void> {
		const req = new DeleteFourn();
		req.CleFourn = id;

		return this.client.delete(req);
	}

}
